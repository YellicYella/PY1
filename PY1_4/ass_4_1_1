def computepay(H,R):
if H>40:
    pay=40*R+(H-40)*1.5*R
else:
    pay=H*R
return pay


hrs=input('enter hours:\n')
rte=input('enter rate:\n')
try:
    h=float(hrs)
    r=float(rte)
except:
    print('error, enter numeric input')
    quit()

P = computepay(h,r)
print('pay',P)
